version: '2'
services:
  wifi-connect:
    build: ./wifi-connect
  ssb-server:
    build: ./ssb-server
    network_mode: "host"
    volumes:
      - ssbhome:/root/.ssb
  yggdrasil:
    build: ./yggdrasil
    privileged: true

volumes:
  ssbhome:
